apply plugin: 'java'

repositories {
    mavenCentral()
}

configurations {
    lintChecks
}

dependencies {
    compile 'com.android.tools.lint:lint-api:26.1.0'
    compile 'com.android.tools.lint:lint-checks:26.1.0'

    lintChecks files(jar)
}

jar {
    manifest {
        attributes("Lint-Registry": "com.wangzhen.lintchecker.lintjar.LintRegistry")
    }
}